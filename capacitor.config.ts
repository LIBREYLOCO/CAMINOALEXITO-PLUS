import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.libreloco.caminoalexito.plus',
  appName: 'Camino Al Exito PLUS',
  webDir: 'dist'
};

export default config;
